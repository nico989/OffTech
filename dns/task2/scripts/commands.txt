# Q1
# On client machine
ifconfig
arp -n 
apt install traceroute -y
traceroute -n 10.1.1.3
# On cache machine
ifconfig
arp -n 

# Q4
# On cache machine
ifconfig
arp -n 
apt install traceroute -y
traceroute -n 10.1.2.3
# On auth machine
ifconfig
arp -n 
